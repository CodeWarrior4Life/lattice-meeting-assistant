"""Integration tests -- exercise multi-component flows with mocked external surfaces.

Unit tests live in ``tests/test_*.py``; this directory hosts the E2E /
integration suites that thread real assistant primitives through scripted
mocks for the external boundaries (Brain MCP, AdminTransport, cortex).
"""
